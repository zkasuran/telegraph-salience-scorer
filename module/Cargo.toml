[package]
name = "telegraph-scorer"
version = "0.1.0"
edition = "2021"

[lib]
name = "telegraph_scorer"
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
